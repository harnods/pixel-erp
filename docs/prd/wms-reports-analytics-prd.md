<!-- Source: https://jurnal.atlassian.net/wiki/spaces/PD/pages/51213697517/WMS+PRD+1.4+WMS+Reports+and+Analytics
     Confluence page ID: 51213697517 · Space: PD · Fetched: 2026-07-30
     Local mirror of the WMS Reports & Analytics PRD. -->

# [WMS PRD 1.4] WMS Reports and Analytics

**Parent:** [\[WMS One Pager\] Mekari WMS](https://jurnal.atlassian.net/wiki/spaces/PD/pages/51199967496)

**Status:** <custom data-type="status" data-id="id-0">draft</custom>

**Scope:** MVP only

**Pic:** <custom data-type="mention" data-id="id-1">@Fitriani Meizvira</custom> 

# TL;DR

| **What this is** | WMS reporting and analytics, delivered as **two separate pages**: **Analytics page** (dashboard) — metric cards / quickviews. Two sections: **Operational** (live daily) and **Post-Operational** (performance). Every card drills down to detail. **Reports page** — row-level raw data dump, with export. Activities covered: Inbound Timeliness, Inbound Accuracy, Outbound Timeliness, Outbound Accuracy, Product Issuance Rate (naming TBD), Inventory Accuracy. |
| --- | --- |
| **Research basis** |  |
| **Proposed solution** |  |

## Change Log

| **Version** | **Published at** | **Updated By** | **Description** |
| --- | --- | --- | --- |
| v1.0 | <custom data-type="date" data-id="id-2">7/7/2026</custom> | <custom data-type="mention" data-id="id-3">@Fitriani Meizvira</custom> |   |
| v1.1 | <custom data-type="date" data-id="id-4">7/8/2026</custom> | <custom data-type="mention" data-id="id-5">@Fitriani Meizvira</custom> | Restructured into two pages (Analytics + Reports). Analytics split into Operational and Post-Operational sections. Added Operational quickview stories, drill-down behavior, and a proposed filter set (incl. operator filters: Receiver, Putaway PIC, Picker, Packer, Courier). Existing content moved: summary → Analytics (Post-Operational); raw data → Reports; per-report Export consolidated into Reports-page behavior. |
| v1.2 | <custom data-type="date" data-id="id-6">7/24/2026</custom> | <custom data-type="mention" data-id="id-7">@Fitriani Meizvira</custom>  | Fixing Receiving Time formula Fixing Total Putaway formula |
| v1.3 | <custom data-type="date" data-id="id-8">7/28/2026</custom> | <custom data-type="mention" data-id="id-9">@Fitriani Meizvira</custom>  | Removal of SKU filters from Analytics page Removal of some calculation in Accuracy section, since it is overlapping with Volume section Addition of “Refresh” button in Analytics page |
| v1.4 | <custom data-type="date" data-id="id-10">7/30/2026</custom> | <custom data-type="mention" data-id="id-11">@Fitriani Meizvira</custom>  | Removing LM role from RBAC-related requirements Adding Ultimate role from RBAC-related requirements |

# Page Structure & Navigation

Reports and Analytics are **two separate pages** under the WMS module.

**Analytics page (dashboard)**

Summary, metrics, and quickviews of Inbound and Outbound activity. Two sections:

* **Operational** — live, daily. "Today, how many inbound... how many received... how many put away." For running day-to-day operations.
* **Post-Operational** — performance metrics (timeliness, accuracy, velocity, stock accuracy). This is what the prototype covers so far.

**Reports page**

The row-level "dump raw data" of Inbound and Outbound activity, with export. This is where a metric card lands when the user wants to see the underlying rows.

### Drill-down behavior

Every card on the Analytics page connects quickly to its detailed data. The destination differs by section:

| **From (card clicked)** | **Opens** | **Filters carried over** |
| --- | --- | --- |
| **Operational** card, e.g. "10 inbound not yet received" | The **Inbound operational page** (the working list, **not** a report) | Warehouse + the card's stage/state (e.g. Waiting to Receive) + today |
| **Operational** card (Outbound), e.g. "5 waiting to ship" | The **Outbound operational page** (working list) | Warehouse + stage/state + today |
| **Post-Operational** metric, e.g. "average picking time" | The **Reports page** → matching report raw data (e.g. Outbound Timeliness) | The same Date + Warehouse (+ SKU / state) currently set on the dashboard |
| **Post-Operational** accuracy metric, e.g. "SHORT EXPECTED %" | The **Reports page** → Inbound/Outbound Accuracy raw data | Date + Warehouse + the clicked state (e.g. SHORT EXPECTED) |

### Proposed filters

Ideas for the filter set across both pages. Operator-level filters (Receiver, Putaway PIC, Picker, Packer, Courier) reuse the P1 columns already defined in raw data, so they are P1.

| **Filter** | **Analytics – Operational** | **Analytics – Post-Op** | **Reports (raw)** | **Notes** |
| --- | --- | --- | --- | --- |
| Date range | Fixed to "today" (live) | Yes (default D-6 to D) | Yes | Operational is real-time, so no range. Post-Op and Reports share the range. |
| Warehouse | Yes | Yes | Yes | Warehouse Manager limited to assigned warehouses. |
| SKU / Product | Optional | Yes (Accuracy, Issuance) | Yes | Enables lead-time-by-product insight (carried-over feedback). |
| Inbound Receive State / Outbound Fulfillment State | No | Yes (Accuracy) | Yes | MATCH / SHORT / OVER EXPECTED. |
| Stage / State (operational) | Yes | No | No | e.g. Waiting to Receive, In Picking. Drives the operational cards. |
| \[P1\] Receiver / Putaway PIC (Inbound); Picker / Packer / Courier (Outbound) | Yes ("my queue", operators) | No | Yes | Operator-level; seeds the future Employee Performance report. |
| \[P1\] Supplier/Sender (Inbound); Customer/Recipient (Outbound) | No | No | Yes | Party-level filtering on raw data. |

# User Stories — Analytics Page

| **#** | **User Story** | **Details** | **Acceptance Criteria** | **Subtask breakdowns** |
| --- | --- | --- | --- | --- |
| General — Analytics page behavior |
| 1 | Access and shared behavior for the Analytics page | Page location In ERP package, the WMS analytics page/dashboard is located under WMS module, submenu Overview In WMS package, the WMS analytics page/dashboard is the first standalone menu on the left navigation bar (replacing ‘Home’ menu in ERP package) This page has two sections: **Operational** (live daily) and **Post-Operational** (performance) In both Inbound and Outbound tab, have a refresh button to reload data in the analytics page without changing any filters, and also information “Updated at hh:mm” Any change of filters will also affect the “Updated at hh:mm” information Access (RBAC) Roles that can view: Owner, Ultimate, ~~LM~~, Warehouse Manager Owner and/or Ultimate ~~LM~~ can view with any Warehouse filter; Warehouse Manager can only view data filtered by their Warehouse assignments _Notes: LM role is removed per 30 July 2026 due to its actual capability, which is to enable the user to do create and update action. Create and update action is not relevant for this page_ Time and timezone Any timestamps shown must use the warehouse local timezone Filters Separate filters between Post-Operational and Operational section. Filtering will be detailed in sections below Export The Analytics page is on-screen only, so NO EXPORT. Export lives on the Reports page | **AC#1 Access by role** **GIVEN** a user with Owner, LM, or Warehouse Manager role, **WHEN** they open the Analytics page, **THEN** it loads; a user without one of these roles cannot access it.   **AC#2 Warehouse scope by role** **GIVEN** an Owner or LM, **WHEN** they use the Warehouse filter, **THEN** they can select any Warehouse or "All"; and a Warehouse Manager only sees their assigned Warehouses.   **AC#3 Two sections present** **GIVEN** the Analytics page, **WHEN** it loads, **THEN** it shows an Operational section and a Post-Operational section.   **AC#4 Empty state** **GIVEN** a filter with no matching data, **WHEN** the page runs, **THEN** it shows a zero or blank state, not an error. | \[Jira\] |
| Operational section (live daily) |
| 2 | Inbound operational quick view _**As a**_ Warehouse Manager, _**I want**_ to see today's live inbound queue, _**so that**_ I can act on what still needs receiving or putaway right now. | Filters Warehouse Dropdown of “All”, and list of active Warehouse Name Default value For Warehouse Manager → Warehouse that is first assigned to that user (earliest created_at for user x warehouse assignment) For Owner and/or Ultimate ~~LM~~ → “All” _Notes: LM role is removed per 30 July 2026 due to its actual capability, which is to enable the user to do create and update action. Create and update action is not relevant for this page_ Filter must not be null for the data to be able to load Date One date filter only (not a range) The month can only be picked for the last 6 months Default value = today Operator (Receiver / Putaway PIC) Dropdown of Warehouse Manager, Warehouse Operator, Not assigned, and “nAllull” Can only select one value Operator filter WILL NOT apply to Pending Inbound and Closed Inbound If value = All, then show all data without operator filter If value = Not assigned, then show data with no assignee yet Quickview cards **Pending Inbound** — this is to show the Inbound that has not been worked on yet. Operator filter does not apply for this card “Total pending Inbound” (main number) Inbound completion status = PENDING Inbound does not have any non-cancelled Receiving or non-cancelled Putaway task “Should arrive at this date” (main number) Inbound with expected arrival date = filtered date, and completion status = PENDING Inbound does not have any non-cancelled Receiving or non-cancelled Putaway task “Should arrive < this date, but not finished yet” Inbound with expected arrival date < filtered date, and completion status = PENDING Inbound does not have any non-cancelled Receiving or non-cancelled Putaway task Upon clicked, it will open Inbound delivery page Tab Receipts Warehouse = filtered Warehouse Status = PENDING Arrival date = custom date range, with ‘to’ = filtered date and ‘from’ = null **Inbound on receiving** — count of distinct Inbound that is currently on receiving. This card will show three numbers, Total distinct Inbound that is currently on Receiving (main number) Inbound completion status (IN PROGRESS, PARTIALLY COMPLETED) Inbound must have at least one Receiving with status = IN PROGRESS Receiving’s start_at must be within filtered date Inbound that is currently on Receiving, and is supposed to come within filtered date Inbound completion status (IN PROGRESS, PARTIALLY COMPLETED) Inbound must have at least one Receiving with status = IN PROGRESS Receiving’s start_at must be within filtered date Inbound’s expected arrival date = filtered date  Inbound that is currently on Receiving, but is supposed to come earlier than filtered date Inbound completion status (IN PROGRESS, PARTIALLY COMPLETED) Inbound must have at least one Receiving with status = IN PROGRESS Receiving’s start_at must be within filtered date Inbound’s expected arrival date < filtered date  Inbound that is currently on Receiving, but is supposed to come later than filtered date Inbound completion status (IN PROGRESS, PARTIALLY COMPLETED) Inbound must have at least one Receiving with status = IN PROGRESS Receiving’s start_at must be within filtered date Inbound’s expected arrival date > filtered date,  Upon clicked, it will open Inbound delivery page Tab Receiving Warehouse = filtered Warehouse Assignee = filtered assignee Status = IN PROCESS **Inbound on putaway** — count of distinct Inbound that is currently on putaway. This card will show three numbers Total distinct Inbound that is currently on Putaway (main number) Inbound completion status (IN PROGRESS, PARTIALLY COMPLETED) Inbound must have at least one Putaway with status = IN PROGRESS Putaway’s start_at must be within filtered date Inbound that is currently on Putaway, and is supposed to come = filtered date Inbound completion status (IN PROGRESS, PARTIALLY COMPLETED) Inbound must have at least one Putaway with status = IN PROGRESS Putaway’s start_at must be within filtered date Inbound’s expected arrival date = filtered date Inbound that is currently on Putaway, but is supposed to come earlier than filtered date Inbound completion status (IN PROGRESS, PARTIALLY COMPLETED) Inbound must have at least one Putaway with status = IN PROGRESS Putaway’s start_at must be within filtered date Inbound’s expected arrival date < filtered date  Inbound that is currently on Putaway, but is supposed to come later than filtered date Inbound completion status (IN PROGRESS, PARTIALLY COMPLETED) Inbound must have at least one Putaway with status = IN PROGRESS Putaway’s start_at must be within filtered date Inbound’s expected arrival date > filtered date,  Upon clicked, it will open Inbound delivery page Tab Putaway Warehouse = filtered Warehouse Assignee = filtered assignee Status = IN PROCESS **Closed inbound**. Operator filter does not apply for this card Total distinct Inbound that is CLOSED (main number) Inbound closure status = CLOSED or completion status = FULLY COMPLETED closed_at must be within filtered date Total distinct Inbound that is already CLOSED, and is supposed to end in filtered date Inbound closure status = CLOSED or completion status = FULLY COMPLETED closed_at must be within filtered date Inbound’s expected arrival date = filtered date Total distinct Inbound that is already CLOSED, but is supposed to end in earlier date Inbound closure status = CLOSED or completion status = FULLY COMPLETED closed_at must be within filtered date Inbound’s expected arrival date < filtered date  Total distinct Inbound that is already CLOSED, but is supposed to end in later date Inbound closure status = CLOSED or completion status = FULLY COMPLETED closed_at must be within filtered date Inbound’s expected arrival date > filtered date  Upon clicked, it will open Inbound delivery page Tab Receipt Warehouse = filtered Warehouse Status = COMPLETED Arrival date = custom date range, with ‘to’ = filtered date and ‘from’ = null **Inbound with no ongoing action** — number of Inbound that is not actively being worked on Total Inbound with no tasks at all. Operator filter does not apply for this number Inbound completion status (IN PROGRESS, PARTIALLY COMPLETED) Has no (OPEN, IN PROGRESS) Receiving nor (OPEN, IN PROGRESS) Putaway Cannot be clicked Total Inbound with Receiving as open task Inbound completion status (IN PROGRESS, PARTIALLY COMPLETED) Has no IN PROGRESS Receiving Has at least one OPEN Receiving task Upon clicked, open Inbound delivery page Tab Receiving Warehouse = filtered Warehouse Assignee = filtered Assignee Status = Open Total Inbound with Putaway as open task Inbound status = IN PROGRESS Has no IN PROGRESS Putaway Has at least one OPEN Putaway task Upon clicked, open Inbound delivery page Tab Receiving Warehouse = filtered Warehouse Assignee = filtered Assignee Status = Open | **AC#1 Live scope** **GIVEN** the Operational section, **WHEN** it loads, **THEN** each card reflects current open work / today for the selected Warehouse.   **AC#2 Drill-down to operational page** **GIVEN** an inbound quickview card, **WHEN** the user clicks it, **THEN** the Inbound operational page opens, filtered to that stage + Warehouse + today (not a report).   **AC#3 RBAC scope** **GIVEN** a Warehouse Manager, **WHEN** the section loads, **THEN** only their assigned Warehouses are available. | \[Jira\] |
| 3 | Outbound operational quickview _**As a**_ warehouse supervisor, _**I want**_ to see today's live outbound queue, _**so that**_ I can act on what still needs picking, packing, or shipping right now. | Filters Warehouse Dropdown of “All”, and list of active Warehouse Name Default value For Warehouse Manager → Warehouse that is first assigned to that user (earliest created_at for user x warehouse assignment) For Owner and/or Ultimate ~~LM~~ → “All” _Notes: LM role is removed per 30 July 2026 due to its actual capability, which is to enable the user to do create and update action. Create and update action is not relevant for this page_ Filter must not be null for the data to be able to load Date One date filter only (not a range) The month can only be picked for the last 6 months Default value = today Operator (Picker / Packer / Shipper PIC) Dropdown of Warehouse Manager, Warehouse Operator, Not assigned, and “All” Can only select one value Operator filter WILL NOT apply to Pending Outbound and Closed Outbound If value = All, then show all data without operator filter If value = Not assigned, then show data with no assignee yet Quickview cards **Pending Outbound** — this is to show the Inbound that has not been worked on yet. Operator filter does not apply for this card “Total pending Inbound” (main number) Outbound completion status is PENDING Outbound does not have any non-canceled Picking, non-canceled Packing, or non-canceled Shipping task “Should be completed at this date” Outbound with expected completion date = filtered date, and completion status is PENDING Outbound does not have non-canceled Picking, non-canceled Packing, or non-canceled Shipping task “Should be completed < this date, but not finished yet” Outbound with expected completion date < filtered date, and completion status is PENDING Outbound does not have any non-canceled Picking, non-canceled Packing, or non-canceled Shipping task Upon clicked, it will open Outbound delivery page Tab Requests Warehouse = filtered Warehouse Status = PENDING Due date = custom date range, with ‘to’ = filtered date and ‘from’ = null **Outbound on picking** — count of distinct Outbound that is currently on picking. This card will show three numbers, Total distinct Outbound that is currently on Picking (main number) Outbound status in (IN PROGRESS, PARTIALLY COMPLETED) Outbound must have at least one Picking with status = IN PROGRESS Picking start_at must be within filtered date Outbound that is currently on Picking, and is supposed to complete within filtered date Outbound status in (IN PROGRESS, PARTIALLY COMPLETED) Outbound must have at least one Picking with status = IN PROGRESS Picking start_at must be within filtered date Outbound’s expected completed date = filtered date Outbound that is currently on Picking, but is supposed to complete earlier than filtered date Outbound status in (IN PROGRESS, PARTIALLY COMPLETED) Outbound must have at least one Picking with status = IN PROGRESS Picking start_at must be within filtered date Outbound’s expected completed date < filtered date Outbound that is currently on Picking, but is supposed to complete later than filtered date Outbound status in (IN PROGRESS, PARTIALLY COMPLETED) Outbound must have at least one Picking with status = IN PROGRESS Picking start_at must be within filtered date Outbound’s expected completed date > filtered date Upon clicked, it will open Outbound delivery page Tab Picking Warehouse = filtered Warehouse Assignee = filtered assignee Status = IN PROCESS **Outbound on packing** — count of distinct Outbound that is currently on packing. This card will show three numbers, Total distinct Outbound that is currently on Packing (main number) Outbound status in (IN PROGRESS, PARTIALLY COMPLETED) Outbound must have at least one Packing with status = IN PROGRESS Packing start_at must be within filtered date Outbound that is currently on Packing, and is supposed to complete within filtered date Outbound status in (IN PROGRESS, PARTIALLY COMPLETED) Outbound must have at least one Packing with status = IN PROGRESS Packing start_at must be within filtered date Outbound’s expected completed date = filtered date Outbound that is currently on Packing, but is supposed to complete earlier than filtered date Outbound status in (IN PROGRESS, PARTIALLY COMPLETED) Outbound must have at least one Packing with status = IN PROGRESS Packing start_at must be within filtered date Outbound’s expected completed date < filtered date Outbound that is currently on Packing, but is supposed to complete later than filtered date Outbound status in (IN PROGRESS, PARTIALLY COMPLETED) Outbound must have at least one Packing with status = IN PROGRESS Packing start_at must be within filtered date Outbound’s expected completed date > filtered date Upon clicked, it will open Outbound delivery page Tab Packing Warehouse = filtered Warehouse Assignee = filtered assignee Status = IN PROCESS **Outbound on Shipping** — count of distinct Outbound that is currently on Shipping. This card will show, Total distinct Outbound that is currently on Shipping (main number) Outbound status in (IN PROGRESS, PARTIALLY COMPLETED) Outbound must have at least one Shipping with status = IN PROGRESS Packing start_at must be within filtered date Outbound that is currently on Shipping, and is supposed to complete within filtered date Outbound status in (IN PROGRESS, PARTIALLY COMPLETED) Outbound must have at least one Shipping with status = IN PROGRESS Shipping start_at must be within filtered date Outbound’s expected completed date = filtered date Outbound that is currently on Shipping, but is supposed to complete earlier than filtered date Outbound status in (IN PROGRESS, PARTIALLY COMPLETED) Outbound must have at least one Shipping with status = IN PROGRESS Packing start_at must be within filtered date Outbound’s expected completed date < filtered date Outbound that is currently on Shipping, but is supposed to complete later than filtered date Outbound status in (IN PROGRESS, PARTIALLY COMPLETED) Outbound must have at least one Shipping with status = IN PROGRESS Shipping start_at must be within filtered date Outbound’s expected completed date > filtered date NOT CLICKABLE or Upon clicked, it will open Outbound shipped page Tab Shipped Warehouse = filtered Warehouse Assignee = filtered assignee Status = IN PROCESS **Closed outbound**. Operator filter does not apply for this card Total distinct Outbound that is CLOSED (main number) Outbound closure status = CLOSED or completion status = FULLY COMPLETED closed_at must be within filtered date Total distinct Outbound that is already CLOSED, and is supposed to end in filtered date Outbound status = CLOSED or completion status = FULLY COMPLETED closed_at must be within filtered date Outbound’s expected completion date = filtered date Total distinct Outbound that is already CLOSED, but is supposed to end in earlier date Outbound status = CLOSED or completion status = FULLY COMPLETED closed_at must be within filtered date Outbound’s expected completion date < filtered date Total distinct Outbound that is already CLOSED, but is supposed to end in later date Outbound status = CLOSED or completion status = FULLY COMPLETED closed_at must be within filtered date Outbound’s expected completion date > filtered date Upon clicked, it will open Outbound delivery page Tab Requests Warehouse = filtered Warehouse Status = COMPLETED Due = custom date range, with ‘to’ = filtered date and ‘from’ = null **Outbound with no ongoing action** — Outbound that is not actively being worked on Total Outbound with no tasks at all. Operator filter does not apply for this number Outbound status in (IN PROGRESS, PARTIALLY COMPLETED) Has no (OPEN, IN PROGRESS) Picking, nor (OPEN, IN PROGRESS) Packing, nor (OPEN, IN PROGRESS) Shipping Cannot be clicked Total Outbound with Picking as open task Outbound status in (IN PROGRESS, PARTIALLY COMPLETED) Has no IN PROGRESS Picking Has at least one OPEN Picking task Upon clicked, open Outbound delivery page Tab Picking Warehouse = filtered Warehouse Assignee = filtered Assignee Status = Open Total Outbound with Packing as open task Outbound status in (IN PROGRESS, PARTIALLY COMPLETED) Has no IN PROGRESS Packing Has at least one OPEN Packing task Upon clicked, open Outbound delivery page Tab Packing Warehouse = filtered Warehouse Assignee = filtered Assignee Status = Open Total Outbound with Shipping as open task Outbound status in (IN PROGRESS, PARTIALLY COMPLETED) Has no IN PROGRESS Shipping Has at least one OPEN Shipping task Upon clicked, open Outbound delivery page Tab Shipped Warehouse = filtered Warehouse Assignee = filtered Assignee Status = Open | **AC#1 Live scope** **GIVEN** the Operational section, **WHEN** it loads, **THEN** each card reflects current open work / today for the selected Warehouse.   **AC#2 Drill-down to operational page** **GIVEN** an outbound quickview card, **WHEN** the user clicks it, **THEN** the Outbound operational page opens, filtered to that stage + Warehouse + today (not a report).   **AC#3 RBAC scope** **GIVEN** a Warehouse Manager, **WHEN** the section loads, **THEN** only their assigned Warehouses are available. | \[Jira\] |
| Post-Operational section — Inbound Timeliness |
| 4 | Inbound Timeliness metrics _**As a**_ warehouse manager, _**I want**_ to see how long inbounds take at each stage, _**so that**_ I can find bottlenecks and hold receiving SLA. | Filters Date (range, default D-6 until D, must fill, max 366 days) Warehouse Dropdown of “All”, and list of active Warehouse Name Default value For Warehouse Manager → Warehouse that is first assigned to that user (earliest created_at for user x warehouse assignment) For Owner and/or Ultimate ~~LM~~ → “All” _Notes: LM role is removed per 30 July 2026 due to its actual capability, which is to enable the user to do create and update action. Create and update action is not relevant for this page_ Filter must not be null for the data to be able to load Operator (Receiver / Putaway PIC) Dropdown of Warehouse Manager, Warehouse Operator, Not assigned, and “All” Can only select one value Operator filter WILL NOT apply to Open Inbound and Closed Inbound If value = all, then show all data without operator filter If value = Not assigned, then show data with no assignee yet Summary headlines: volume **Total Inbound Created.** Operator filter does not apply for this number number of distinct Inbound created in WMS within the filtered date Inbound closure status != CANCELLED Inbound's created_at is within filtered date number of distinct SKU from Inbound in point a) total qty to be received from Inbound in point a) Upon clicked, open WMS Inbound Timeliness Reports Date filter = same as active date filter in this summary Warehouse = same as active warehouse in this summary Operator = same as active operator filter in this summary  **Total Closed Inbound.** Operator filter does not apply for this number number of distinct Inbound that is already closed within the filtered date Inbound closure status = CLOSED Inbound's closed_at is within filtered date number of distinct SKU from Inbound in point a) total qty to be received from Inbound in point a) Upon clicked, open WMS Inbound Timeliness Reports Date filter = same as active date filter in this summary Warehouse = same as active warehouse in this summary Operator = same as active operator filter in this summary  **Total Receiving.** Showing, number of distinct Receiving activities happening within the filtered date Receiving status = COMPLETED Receiving's completed_at is within filtered date number of distinct SKU from Receiving in point a) average distinct SKU per Receiving total qty from Receiving in point a) Product that is tracked by Batch should be calculated in the Product level (sum of all Batch's qty) average qty per Receiving Upon clicked, open WMS Inbound Timeliness Reports Date filter = same as active date filter in this summary Warehouse = same as active warehouse in this summary Operator = same as active operator filter in this summary  **Total Putaway**. Showing, number of distinct Putaway activities from happening within the filtered date Putaway status = COMPLETED Putaway's completed_at is within filtered date number of distinct SKU from Putaway in point a) average distinct SKU per Putaway total qty from Putaway in point a) Product that is tracked by Batch should be calculated in the Product level (sum of all Batch's qty) average qty per Putaway Upon clicked, open WMS Inbound Timeliness Reports Date filter = same as active date filter in this summary Warehouse = same as active warehouse in this summary Operator = same as active operator filter in this summary  **Inbound, Receiving, Putaway comparison.** Operator filter does not apply for this card Average Receiving created for one Inbound all Inbound with closed_at within filtered date for selected Inbound, get all Receiving with status = COMPLETED Average Receiving task in one Putaway all Putaway with completed_at within filtered date for selected Putaway, get all Receiving with status = COMPLETED Upon clicked, open WMS Inbound Timeliness Reports Date filter = same as active date filter in this summary Warehouse = same as active warehouse in this summary Operator = same as active operator filter in this summary  Summary headlines: duration \[logic to show duration, follow the current development\] **Inbound to Expected Arrival Date.** Operator filter does not apply for this card Get the Inbound which closed_at is within filtered date Get the average of (Inbound expected_date minus Inbound created_at) Get the median of (Inbound expected_date minus Inbound created_at) \[Important\] Shall there be duration which is <= 0, count the value as 0 Upon clicked, open WMS Inbound Timeliness Reports Date filter = same as active date filter in this summary Warehouse = same as active warehouse in this summary Operator = same as active operator filter in this summary  **Inbound to Receiving** → showing time taken to receive an Inbound for the first time, in average and median. Operator filter does not apply for this card Get the Inbound which closed_at is within filtered date For each Inbound, get only the 'first Receiving' task of the Inbound (note: one Inbound can have multiple Receiving. We only want to count using the **Receiving task that is started first**) Average of ('first Receiving' start_at minus Inbound created_at) Median of ('first Receiving' start_at minus Inbound created_at) \[Important\] Shall there be duration which is <= 0, count the value as 0 Upon clicked, open WMS Inbound Timeliness Reports Date filter = same as active date filter in this summary Warehouse = same as active warehouse in this summary Operator = same as active operator filter in this summary  **Receiving time** → How long does receiving process take, in average and median Get all Receiving which finished_at is within filtered date Average of (Receiving finished_at minus Receiving start_at) Median of (Receiving finished_at minus Receiving start_at) \[Important\] Shall there be duration which is <= 0, count the value as 0 Upon clicked, open WMS Inbound Timeliness Reports Date filter = same as active date filter in this summary Warehouse = same as active warehouse in this summary Operator = same as active operator filter in this summary  **Putaway time (Receiving to Putaway)** → The time from the end of Receiving to the end of Putaway, to show how long does it need for a product to be put safely in the warehouse after it is received. Calculation critera, Get all Receiving which finished_at is within filtered date For each Receiving, get all Putaway which status = COMPLETED Note: For this card, Operator filter applies to Putaway PIC Average of (Putaway finish_at minus Receiving finish_at) Median of (Putaway finish_at minus Receiving finish_at) \[Important\] Shall there be duration which is <= 0, count the value as 0 If user does not have any Putaway at all (all WH’s skip_putaway = TRUE), then show “-” in the UI Upon clicked, open WMS Inbound Timeliness Reports Date filter = same as active date filter in this summary Warehouse = same as active warehouse in this summary Operator = same as active operator filter in this summary  **Inbound cycle time (dock to stock)** → The end-to-end time for Inbound to be created until closed. Operator filter does not apply for this card. Calculate: Get all Inbound which closed_at is within filtered date, Average (Inbound closed_at minus Inbound created_at) Median (Inbound closed_at minus Inbound created_at) \[Important\] Shall there be duration which is <= 0, count the value as 0 Upon clicked, open WMS Inbound Timeliness Reports Date filter = same as active date filter in this summary Warehouse = same as active warehouse in this summary Operator = same as active operator filter in this summary  **Inbound Timeliness** → the gap between actual Inbound closing time and Inbound expected arrival date. The closer the value to zero, the more precise the expectation and the actual condition of Inbound time. Operator filter does not apply for this card Get all Inbound which closed_at is within filtered date, Average (Inbound closed_at minus Inbound expected_arrival_date) Median (Inbound closed_at minus Inbound expected_arrival_date) Notes: in this metrics, positive value shows that actual inbound is later than expected inbound. Negative value shows that actual inbound is earlier than expected inbound Upon clicked, open WMS Inbound Timeliness Reports Date filter = same as active date filter in this summary Warehouse = same as active warehouse in this summary Operator = same as active operator filter in this summary  | **AC#1 Cancelled excluded** **GIVEN** inbounds where some are CANCELLED, **WHEN** the volume headlines are computed, **THEN** CANCELLED inbounds are excluded from every count.   **AC#2 Volume detail** **GIVEN** a volume headline, **WHEN** it loads, **THEN** it shows the count, distinct SKU, and total qty, with batch-tracked products summed at the product level.   **AC#3 Duration statistic** **GIVEN** stage durations, **WHEN** displayed, **THEN** each duration shows both average and median across the inbounds in scope, in hours and minutes.   **AC#4 First receiving used** **GIVEN** an inbound with multiple receiving tasks, **WHEN** Inbound to Receiving time is computed, **THEN** only the first receiving task's start is used.   **AC#5 Non-positive duration** **GIVEN** a computed duration less than or equal to 0, **WHEN** it is aggregated, **THEN** it is counted as 0.   **AC#6 Drill-down** **GIVEN** a metric card, **WHEN** clicked, **THEN** the Reports page Inbound Timeliness raw data opens with the same Date and Warehouse filters. | \[Jira\] |
| Post-Operational section — Inbound Accuracy |
| 5 | Inbound Accuracy metrics _**As a**_ warehouse manager, _**I want**_ to see whether we received what was expected, _**so that**_ I can catch short, over, or missed receipts. | Filters Date (range, default D-6 until D, must fill, max 366 days) Warehouse Dropdown of “All”, and list of active Warehouse Name Default value For Warehouse Manager → Warehouse that is first assigned to that user (earliest created_at for user x warehouse assignment) For Owner and/or Ultiamte ~~LM~~ → “All” _Notes: LM role is removed per 30 July 2026 due to its actual capability, which is to enable the user to do create and update action. Create and update action is not relevant for this page_ Filter must not be null for the data to be able to load ~~SKU (default "All"; either "All" or 1 <= x <= 10 SKUs)~~ Removed from the scope as per <custom data-type="date" data-id="id-12">7/28/2026</custom> ~~Source of Inbound~~ Removed from the scope as per <custom data-type="date" data-id="id-13">7/28/2026</custom> ~~Dropdown of “All”, and list of Inbound source. User might choose 1 <= x <= 10 Source~~ ~~Default value = All~~ ~~Filter must not be null for the data to be able to load~~ ~~Inbound Receive State (default "All"; MATCH EXPECTED, SHORT EXPECTED, OVER EXPECTED; can only be used if SKU filter = All)~~ Removed from the scope as per <custom data-type="date" data-id="id-14">7/28/2026</custom> Summary headlines **Total Closed Inbound.** Showing, ~~number of distinct Inbound that is already closed within the filtered date~~ ~~Inbound status = CLOSED~~ ~~Inbound's closed_at is within filtered date~~ ~~number of distinct SKU from Inbound in point a)~~ ~~total qty to be received from Inbound in point a)~~ Already included in Timeliness/Volume section above, removal per <custom data-type="date" data-id="id-15">7/28/2026</custom> Upon clicked, open WMS Inbound Accuracy Reports Date filter = same as active date filter in this summary Warehouse = same as active warehouse in this summary SKU = same as active SKU in this summary Source = same as active Source filter in this summary **Inbound Receive State**. Get all Inbound which is closed within the filtered date, and then show, number and percentage of closed Inbound with completion = MATCH EXPECTED / addition of on-hand stock = Inbound qty number and percentage of closed Inbound with completion = SHORT EXPECTED / addition of on-hand stock < Inbound qty number and percentage of closed Inbound with completion = OVER EXPECTED  / addition of on-hand stock > Inbound qty Upon clicked, open WMS Inbound Accuracy Reports Date filter = same as active date filter in this summary Warehouse = same as active warehouse in this summary SKU = same as active SKU in this summary Source = same as active Source filter in this summary **Total Receive Qty** ~~Get all Inbound which is closed within the filtered date,~~ ~~Get all finished Receiving from all Inbound in a)~~, Will be using the definition in Timeliness/Volume section above, removal per <custom data-type="date" data-id="id-16">7/28/2026</custom> then sum (qty from filtered Product from all Receiving) → show in number, and percentage (divided by filtered Product’s all Inbound Qty) Product that is tracked by Batch should be calculated in the Product level (sum of all Batch's qty) Upon clicked, open WMS Inbound Accuracy Reports Date filter = same as active date filter in this summary Warehouse = same as active warehouse in this summary SKU = same as active SKU in this summary Source = same as active Source filter in this summary **Total Putaway Qty** ~~Get all Inbound which is closed within the filtered date,~~ ~~Get all finished Putaway from Inbound in a),~~ Will be using the definition in Timeliness/Volume section above, removal per  <custom data-type="date" data-id="id-17">7/28/2026</custom> then sum (qty from filtered Product from all Putaway) → show in number, and percentage (divided by filtered Product’s all Inbound Qty) Product that is tracked by Batch should be calculated in the Product level (sum of all Batch's qty) If user does not have any Putaway at all (all WH’s skip_putaway = TRUE), then show “-” in the UI Upon clicked, open WMS Inbound Accuracy Reports Date filter = same as active date filter in this summary Warehouse = same as active warehouse in this summary SKU = same as active SKU in this summary Source = same as active Source filter in this summary Special notes/Edge cases Receive rate might exceed 100%, due to the option to receive more than Inbound Qty Shall there be a blind or ad-hoc receiving with no expected qty (Inbound qty = 0), exclude the Receiving from Total Receiving Percentage, but include it still in the Total Receiving Qty Filter to a single SKU: Distinct SKU = 1; other headlines reflect only that SKU | **AC#1 Closed inbound basis** **GIVEN** inbounds in the filtered date, **WHEN** the summary is computed, **THEN** only inbounds with status CLOSED and closed-at within the filtered date are included; CANCELLED inbounds are excluded.   **AC#2 Receive state breakdown** **GIVEN** the SKU filter is set to "All", **WHEN** the summary loads, **THEN** Inbound Receive State shows the count and percentage of closed inbounds for MATCH EXPECTED, SHORT EXPECTED, and OVER EXPECTED.   **AC#3 Receive state hidden per SKU** **GIVEN** the SKU filter is set to specific SKUs (not "All"), **WHEN** the summary loads, **THEN** the Inbound Receive State breakdown shows the NaN state instead of percentages.   **AC#4 Receive and Putaway percentage** **GIVEN** Total Inbound Qty greater than 0, **WHEN** Total Receive Qty and Total Putaway Qty are shown, **THEN** each shows its quantity and its percentage of Total Inbound Qty; over-receipt may exceed 100% and is not capped.   **AC#5 Blind receiving handling** **GIVEN** a blind or ad-hoc receiving with no expected qty (Inbound Qty = 0), **WHEN** the percentages are computed, **THEN** that receiving is excluded from the percentage but still counted in Total Receive Qty.   **AC#6 Batch at product level** **GIVEN** a product tracked by Batch, **WHEN** Receive or Putaway qty is summed, **THEN** it is aggregated at the product level (sum of all batch qty). | \[Jira\] |
| Post-Operational section — Outbound Timeliness |
| 6 | Outbound Timeliness metrics _**As a**_ warehouse manager, _**I want**_ to see how long outbounds take at each stage, _**so that**_ I can find bottlenecks and hold shipping SLA. | Filters Date (range, default D-6 until D, must fill, max 366 days) Warehouse Dropdown of “All”, and list of active Warehouse Name Default value For Warehouse Manager → Warehouse that is first assigned to that user (earliest created_at for user x warehouse assignment) For Owner and/or Ultimate ~~LM~~ → “All” Filter must not be null for the data to be able to load Operator (Picker / Packer / Shipping PIC) Dropdown of Warehouse Manager, Warehouse Operator, Not assigned, and “all” Can only select one value Operator filter WILL NOT apply to Pending Outbound and Closed Outbound If value = all, then show all data without operator filter If value = Not assigned, then show data with no assignee yet Outbound flow: Created → Picking → Packing → Shipping → Closed Summary headlines: volume **Total Outbound Created.** Operator filter does not apply for this number number of distinct Outbound created in WMS within the filtered date Outbound closure status != CANCELLED Outbound's created_at is within filtered date number of distinct SKU from Outbound in point a) total qty to be shipped from Outbound in point a) Upon clicked, open WMS Outbound Timeliness Reports Date filter = same as active date filter in this summary Warehouse = same as active warehouse filter in this summary Operator = same as active operator filter in this summary  **Total Closed Outbound.** Operator filter does not apply for this number number of distinct Outbound that is already closed within the filtered date Outbound closure status = CLOSED Outbound's closed_at is within filtered date number of distinct SKU from Outbound in point a) total qty to be shipped from Outbound in point a) Upon clicked, open WMS Outbound Timeliness Reports Date filter = same as active date filter in this summary Warehouse = same as active warehouse filter in this summary Operator = same as active operator filter in this summary  **Total Picking.** Showing, number of distinct Picking activities happening within the filtered date Picking status = COMPLETED Picking's completed_at is within filtered date number of distinct SKU from Picking in point a) average distinct SKU per Picking total qty from Picking in point a) Product that is tracked by Batch should be calculated in the Product level (sum of all Batch's qty) average qty per Picking Upon clicked, open WMS Outbound Timeliness Reports Date filter = same as active date filter in this summary Warehouse = same as active warehouse filter in this summary Operator = same as active operator filter in this summary  **Total Packing**. Showing, number of distinct Packing activities happening within the filtered date Packing status = COMPLETED Packing's completed_at is within filtered date number of distinct SKU from Packing in point a) average distinct SKU per Packing total qty from Packing in point a) Product that is tracked by Batch should be calculated in the Product level (sum of all Batch's qty) average qty per Packing Upon clicked, open WMS Outbound Timeliness Reports Date filter = same as active date filter in this summary Warehouse = same as active warehouse filter in this summary Operator = same as active operator filter in this summary  **Total Shipping**. Showing, number of distinct Shipping activities happening within the filtered date Shipping status = COMPLETED Shipping's completed_at is within filtered date number of distinct SKU from Shipping in point a) average distinct SKU per Shipping total qty from Shipping in point a) Product that is tracked by Batch should be calculated in the Product level (sum of all Batch's qty) average qty per Shipping Upon clicked, open WMS Outbound Timeliness Reports Date filter = same as active date filter in this summary Warehouse = same as active warehouse filter in this summary Operator = same as active operator filter in this summary  **Outbound, Picking, Packing, Shipping comparison.** Operator filter does not apply for this card Average Picking created for one Outbound all Outbound with closed_at within filtered date for selected Outbound, get all Picking with status = COMPLETED Average Packing task for one Outbound all Outbound with closed_at within filtered date for selected Outbound, get distinct Packing with status = COMPLETED Average Shipping task for one Outbound (all Outbound with closed_at within filtered date) all Outbound with closed_at within filtered date for selected Outbound, get distinct Shipping with status = COMPLETED Upon clicked, open WMS Outbound Timeliness Reports Date filter = same as active date filter in this summary Warehouse = same as active warehouse filter in this summary Operator = same as active operator filter in this summary  Summary headlines: duration \[logic to show duration, follow the current development\] **Outbound to Expected Completion Date.** Operator filter does not apply for this card Get the Outbound which closed_at is within filtered date Average of (Outbound expected_completion_date minus Outbound created_at) Median of (Outbound expected_completion_date minus Outbound created_at) \[Important\] Shall there be duration which is <= 0, count the value as 0 Upon clicked, open WMS Outbound Timeliness Reports Date filter = same as active date filter in this summary Warehouse = same as active warehouse filter in this summary Operator = same as active operator filter in this summary  **Outbound to Picking** → showing time taken to pick an Outbound for the first time, in average and median Get the Outbound which closed_at is within filtered date Get only the 'first Picking' task of the Outbound (note: one Outbound can have multiple Picking. We only want to count using the **Picking task that is first started**) Average of ('first Picking' start_at minus Outbound created_at) Median of ('first Picking' start_at minus Outbound created_at) \[Important\] Shall there be duration which is <= 0, count the value as 0 Upon clicked, open WMS Outbound Timeliness Reports Date filter = same as active date filter in this summary Warehouse = same as active warehouse filter in this summary Operator = same as active operator filter in this summary  **Picking time** → How long does picking process take, in average and median Get all Picking which finished_at is within filtered date Average of (Picking finished_at minus Picking start_at) Median of (Picking finished_at minus Picking start_at) \[Important\] Shall there be duration which is <= 0, count the value as 0 Upon clicked, open WMS Outbound Timeliness Reports Date filter = same as active date filter in this summary Warehouse = same as active warehouse filter in this summary Operator = same as active operator filter in this summary  If user does not have any Picking at all (all WH’s skip_picking = TRUE), then show “-” in the UI **Packing time** The time from the start of Packing to the end of Packing. Calculation criteria, Get all Packing which finished_at is within filtered date Average of (Packing finish_at minus Packing start_at) Median of (Packing finish_at minus Packing start_at) \[Important\] Shall there be duration which is <= 0, count the value as 0 Upon clicked, open WMS Outbound Timeliness Reports Date filter = same as active date filter in this summary Warehouse = same as active warehouse filter in this summary Operator = same as active operator filter in this summary  **Shipping time (Packing to Shipping)** → The time from the end of Packing to shipped. Operator filter applies on the Shipping side. Calculation criteria, Get all Shipping which shipped_at is within filtered date From selected Shipping, get all Packing with status = COMPLETED, related to the Shipping Average of (Shipping shipped_at minus Packing finish_at) Median of (Shipping shipped_at minus Packing finish_at) \[Important\] Shall there be duration which is <= 0, count the value as 0 Upon clicked, open WMS Outbound Timeliness Reports Date filter = same as active date filter in this summary Warehouse = same as active warehouse filter in this summary Operator = same as active operator filter in this summary  **Outbound cycle time (order to ship)** → The end-to-end time for Outbound to be created until closed. Operator filter does not apply for this card. Calculate: Get all Outbound which closed_at is within filtered date, Average (Outbound closed_at minus Outbound created_at) Median (Outbound closed_at minus Outbound created_at) \[Important\] Shall there be duration which is <= 0, count the value as 0 Upon clicked, open WMS Outbound Timeliness Reports Date filter = same as active date filter in this summary Warehouse = same as active warehouse filter in this summary Operator = same as active operator filter in this summary **Outbound Timeliness** → the gap between actual Outbound closing time and Outbound expected ship date. Operator filter does not apply for this card. The closer the value to zero, the more precise the expectation and the actual condition of Outbound time Get all Outbound which closed_at is within filtered date, Average (Outbound closed_at minus Outbound expected_ship_date) Median (Outbound closed_at minus Outbound expected_ship_date) Notes: in this metrics, positive value shows that actual outbound is later than expected outbound. Negative value shows that actual outbound is earlier than expected outbound Upon clicked, open WMS Outbound Timeliness Reports Date filter = same as active date filter in this summary Warehouse = same as active warehouse filter in this summary Operator = same as active operator filter in this summary | **AC#1 Cancelled excluded** **GIVEN** outbounds where some are CANCELLED, **WHEN** the volume headlines are computed, **THEN** CANCELLED outbounds are excluded from every count.   **AC#2 Volume detail** **GIVEN** a volume headline (Created, Closed, Picking, Packing, Shipping), **WHEN** it loads, **THEN** it shows the activity count, distinct SKU, and total qty, with batch-tracked products summed at the product level.   **AC#3 Duration statistic** **GIVEN** stage durations, **WHEN** displayed, **THEN** each duration shows both average and median, in hours and minutes.   **AC#4 First picking used** **GIVEN** an outbound with multiple picking tasks, **WHEN** Outbound to Picking time is computed, **THEN** only the first picking task's start is used.   **AC#5 Non-positive duration** **GIVEN** a computed duration less than or equal to 0, **WHEN** it is aggregated, **THEN** it is counted as 0.   **AC#6 Timeliness sign** **GIVEN** the Outbound Timeliness metric, **WHEN** it is shown, **THEN** a positive value means actual is later than expected, and a negative value means earlier. | \[Jira\] |
| Post-Operational section — Outbound Accuracy |
| 7 | Outbound Accuracy metrics _**As a**_ warehouse manager, _**I want**_ to see whether we shipped what was ordered, _**so that**_ I can catch short or wrong shipments. | Filters Date (range, default D-6 until D, must fill, max 366 days) Warehouse Dropdown of “All”, and list of active Warehouse Name Default value For Warehouse Manager → Warehouse that is first assigned to that user (earliest created_at for user x warehouse assignment) For Owner and/or Ultimate ~~LM~~ → “All” _Notes: LM role is removed per 30 July 2026 due to its actual capability, which is to enable the user to do create and update action. Create and update action is not relevant for this page_ Filter must not be null for the data to be able to load ~~SKU (default "All"; either "All" or 1 <= x <= 10 SKUs)~~ Removal per <custom data-type="date" data-id="id-18">7/28/2026</custom> Operator (Picker / Packer / Shipping PIC) Dropdown of Warehouse Manager, Warehouse Operator, Not assigned, and “all” Can only select one value Operator filter WILL NOT apply to Pending Outbound and Closed Outbound If value = all, then show all data without operator filter If value = Not assigned, then show data with no assignee yet Summary headlines **Total Closed Outbound.** Showing, ~~number of distinct Outbound that is already closed within the filtered date~~ ~~Outbound status = CLOSED~~ ~~Outbound's closed_at is within filtered date~~ ~~number of distinct SKU from Outbound in point a)~~ ~~total qty to be shipped (ordered qty) from Outbound in point a)~~ Already included in Timeliness/Volume section above, removal per <custom data-type="date" data-id="id-19">7/28/2026</custom> Upon clicked, open WMS Outbound Accuracy Reports Date filter = same as active date filter in this summary Warehouse = same as active warehouse in this summary SKU = same as active SKU in this summary Operator = same as active Operator filter in this summary **Total Picked Qty** ~~Get all Outbound which is closed within the filtered date,~~ ~~Get all finished Picking from all Outbound in a)~~, Will be using the definition in Timeliness/Volume section above, removal per <custom data-type="date" data-id="id-20">7/28/2026</custom> then sum (qty from all Product from all Picking) → show in number, and percentage (divided by total Outbound Qty) Product that is tracked by Batch should be calculated in the Product level (sum of all Batch's qty) Upon clicked, open WMS Outbound Accuracy Reports Date filter = same as active date filter in this summary Warehouse = same as active warehouse in this summary SKU = same as active SKU in this summary Operator = same as active Operator filter in this summary **Total Packed Qty** ~~Get all Outbound which is closed within the filtered date,~~ ~~Get all finished Packing from all Outbound in a)~~,Will be using the definition in Timeliness/Volume section above, removal per <custom data-type="date" data-id="id-21">7/28/2026</custom> then sum (qty from all Product from all Packing) → show in number, and percentage (divided by total Outbound Qty) Product that is tracked by Batch should be calculated in the Product level (sum of all Batch's qty) Upon clicked, open WMS Outbound Accuracy Reports Date filter = same as active date filter in this summary Warehouse = same as active warehouse in this summary SKU = same as active SKU in this summary Operator = same as active Operator filter in this summary **Total Shipped Qty** ~~Get all Outbound which is closed within the filtered date,~~ ~~Get all Shipping from all Outbound in a),~~ Will be using the definition in Timeliness/Volume section above, removal per  <custom data-type="date" data-id="id-22">7/28/2026</custom> then sum (qty from all Product from all Shipping) → show in number, and percentage (divided by total Outbound Qty) Product that is tracked by Batch should be calculated in the Product level (sum of all Batch's qty) Upon clicked, open WMS Outbound Accuracy Reports Date filter = same as active date filter in this summary Warehouse = same as active warehouse in this summary SKU = same as active SKU in this summary Operator = same as active Operator filter in this summary Special notes/Edge cases Fulfillment rate might exceed 100%, due to over-pick or over-ship Fulfillment percentage may differ across stages (picked vs packed vs shipped); show each separately Filter to a single SKU: Distinct SKU = 1; other headlines reflect only that SKU | **AC#1 Closed outbound basis** **GIVEN** outbounds in the filtered date, **WHEN** the summary is computed, **THEN** only outbounds with status CLOSED and closed-at within the filtered date are included; CANCELLED outbounds are excluded.   **AC#2 Fulfillment state breakdown** **GIVEN** the SKU filter is set to "All", **WHEN** the summary loads, **THEN** Outbound Fulfillment State shows the count and percentage of closed outbounds for MATCH EXPECTED, SHORT EXPECTED, and OVER EXPECTED.   **AC#3 Fulfillment state hidden per SKU** **GIVEN** the SKU filter is set to specific SKUs (not "All"), **WHEN** the summary loads, **THEN** the Outbound Fulfillment State breakdown shows the NaN state instead of percentages.   **AC#4 Stage percentage** **GIVEN** Total Outbound Qty greater than 0, **WHEN** Total Picked Qty, Total Packed Qty, and Total Shipped Qty are shown, **THEN** each shows its quantity and its percentage of Total Outbound Qty; over-fulfilment may exceed 100% and is not capped.   **AC#5 Batch at product level** **GIVEN** a product tracked by Batch, **WHEN** stage qty is summed, **THEN** it is aggregated at the product level (sum of all batch qty). | \[Jira\] |

# User Stories — Reports Page

| **#** | **User Story** | **Importance** | **Design** | **Details** | **Acceptance Criteria** | **Subtask breakdowns** |
| --- | --- | --- | --- | --- | --- | --- |
| General — Reports page behavior |
| 10 | Access, export, and shared behavior for the Reports page | <custom data-type="status" data-id="id-23">Must Have</custom> | \[TBD\] | Page location In ERP package, the WMS Report is located in Report module, new WMS submenu In WMS package, the WMS Report is located in Report module Access (RBAC) Roles that can view: Owner, Ultimate, ~~LM~~, Warehouse Manager Owner, Ultimate, ~~LM~~ can view with any Warehouse filter; Warehouse Manager can only view data filtered by their Warehouse assignments _Notes: LM role is removed per 30 July 2026 due to its actual capability, which is to enable the user to do create and update action. Create and update action is not relevant for this page_ Time and timezone Any timestamps shown must use the warehouse local timezone Filters Filtering will be detailed in sections below Export Export is available in report page Exportable to CSV (P0) and XLS/XLSX (P1) Export respects the active filters: the file contains exactly the filtered rows Empty and loading states When no data matches the filter, show a zero or blank state, not an error | **AC#1 Access by role** **GIVEN** a user with Owner, LM, or Warehouse Manager role, **WHEN** they open the Reports page, **THEN** it loads; a user without one of these roles cannot access it.   **AC#2 Warehouse scope by role** **GIVEN** an Owner or LM, **WHEN** they use the Warehouse filter, **THEN** they can select any Warehouse or "All"; and a Warehouse Manager only sees their assigned Warehouses.   **AC#3 Export scope and formats** **GIVEN** a raw report, **WHEN** the user exports, **THEN** only raw data is exportable, in CSV (P0) and XLS/XLSX (P1).   **AC#4 Export respects filters** **GIVEN** active filters, **WHEN** the user exports, **THEN** the file contains exactly the filtered rows.   **AC#5 Empty state** **GIVEN** a filter with no matching rows, **WHEN** the report runs, **THEN** it shows a zero or blank state, not an error. | \[Jira\] |
| Inbound (raw data) |
| 11 | Inbound Timeliness — raw data _**As an**_ analyst, _**I want**_ row-level inbound timeliness data I can export, _**so that**_ I can audit specific inbounds and run my own analysis. | <custom data-type="status" data-id="id-24">Must Have</custom> | \[TBD\] | Filters Date (range, default D-6 until D, must fill, max 366 days) Warehouse Dropdown of “All”, and list of active Warehouse Name Default value For Warehouse Manager → Warehouse that is first assigned to that user (earliest created_at for user x warehouse assignment) For Owner and/or ~~LM~~ Ultimate→ “All” _Notes: LM role is removed per 30 July 2026 due to its actual capability, which is to enable the user to do create and update action. Create and update action is not relevant for this page_ Filter must not be null for the data to be able to load Operator (Receiver / Putaway PIC) Dropdown of Warehouse Manager, Warehouse Operator, Not assigned, and “All” Can only select one value ~~Operator filter WILL NOT apply to Open Inbound and Closed Inbound~~ If value = all, then show all data without operator filter If value = Not assigned, then show data with no assignee yet Raw data columns Get all Inbound which is closed within the filtered date. Timestamp should be in hours, minutes, and seconds. In this report, queried data should be unique per Receiving **Warehouse Name** **Inbound ID** **Inbound Expected Arrival At** **Inbound Closing time** – One Inbound multiple Receiving will have same value **Receiving ID** – If an Inbound has multiple receiving events, have multiple rows (according to the Receiving) **\[P1\] Receiver Name** **Receiving Start** **Receiving Finish** **\[P1\] Putaway PIC Name** **Putaway ID** **Putaway Start** **Putaway Finish** Edge cases Missing stage timestamps show as blank, not zero or epoch | **AC#1 Columns present** **GIVEN** the raw data view, **WHEN** it loads, **THEN** it shows Warehouse Name, Inbound ID, Inbound Closing Time, Receiving ID, Receiver Name (P1), Receiving Start, Receiving Finish, Putaway PIC Name (P1), Putaway ID, Putaway Start, Putaway Finish.   **AC#2 Row granularity** **GIVEN** an inbound with multiple receiving events, and receivings with multiple putaways, **WHEN** raw data loads, **THEN** there is one row per Receiving and one row per Putaway, with inbound-level fields repeated.   **AC#3 Blank not zero** **GIVEN** an inbound with a stage not yet done, **WHEN** raw data shows that timestamp, **THEN** the cell is blank, not zero or epoch. | \[Jira\] |
| 12 | Inbound Accuracy — raw data _**As an**_ analyst, _**I want**_ row-level inbound accuracy data I can export, _**so that**_ I can trace fill rate down to each inbound and SKU. | <custom data-type="status" data-id="id-25">Must Have</custom> | \[TBD\] | Filters Date (range, default D-6 until D, must fill, max 366 days) Warehouse Dropdown of “All”, and list of active Warehouse Name Default value For Warehouse Manager → Warehouse that is first assigned to that user (earliest created_at for user x warehouse assignment) For Owner and/or ~~LM~~ Ultimate→ “All” _Notes: LM role is removed per 30 July 2026 due to its actual capability, which is to enable the user to do create and update action. Create and update action is not relevant for this page_ Filter must not be null for the data to be able to load SKU (default "All"; either "All" or 1 <= x <= 10 SKUs) Source of Inbound Dropdown of “All”, and list of Inbound source. User mighht choose 1 <= x <= 10 Source Default value = All Filter must not be null for the data to be able to load Inbound Receive State (default "All"; MATCH EXPECTED, SHORT EXPECTED, OVER EXPECTED; can only be used if SKU filter = All) Raw data columns Get all Inbound which is closed within the filtered date. In this report, queried data should be unique per Product and Receiving **\[P1\] Supplier/Sender Name** **Warehouse Name** **Inbound ID** – One Inbound with multiple SKU lines will have multiple rows **Product** **Name** \[Important\] Will only have value if the SKU filter != All. Otherwise, will be left blank **Inbound Qty** **\[P1\] Receiver Name** **Receiving ID –** One Inbound with multiple Receiving lines will have multiple rows **Receiving Qty** **\[P1\] Putaway PIC** **Putaway ID** **Putaway Qty** Edge cases Batch or serial tracked SKU: line Receive qty sums across batches or serial numbers Wrong item (not on PO): row with PO qty 0 and Receive qty greater than 0 | **AC#1 Row grain** **GIVEN** an inbound with multiple SKU lines, receivings, or putaways, **WHEN** raw data loads, **THEN** there is one row per Inbound line and per Receiving or Putaway, with inbound-level fields repeated, reflecting the filters.   **AC#2 Columns present** **GIVEN** the raw data view, **WHEN** it loads, **THEN** it shows Supplier/Sender Name (P1), Warehouse Name, Inbound ID, Product Name, Inbound Qty, Receiver Name (P1), Receiving ID, Receiving Qty, Putaway PIC (P1), Putaway ID, Putaway Qty.   **AC#3 Product Name per SKU filter** **GIVEN** the SKU filter is "All", **WHEN** raw data loads, **THEN** Product Name is left blank; and when the SKU filter is specific SKUs, Product Name is populated. | \[Jira\] |
| Outbound (raw data) |
| 13 | Outbound Timeliness — raw data _**As an**_ analyst, _**I want**_ row-level outbound timeliness data I can export, _**so that**_ I can audit specific outbounds and run my own analysis. | <custom data-type="status" data-id="id-26">Must Have</custom> | \[TBD\] | Filters Date (range, default D-6 until D, must fill, max 366 days) Warehouse Dropdown of “All”, and list of active Warehouse Name Default value For Warehouse Manager → Warehouse that is first assigned to that user (earliest created_at for user x warehouse assignment) For Owner and/or ~~LM~~ Ultimate → “All” _Notes: LM role is removed per 30 July 2026 due to its actual capability, which is to enable the user to do create and update action. Create and update action is not relevant for this page_ Filter must not be null for the data to be able to load Operator (Picker / Packer / Shipping PIC) Dropdown of Warehouse Manager, Warehouse Operator, Not assigned, and “all” Can only select one value Operator filter WILL NOT apply to Pending Outbound and Closed Outbound If value = all, then show all data without operator filter If value = Not assigned, then show data with no assignee yet Raw data columns Get all Outbound which is closed within the filtered date. Timestamp should be in hours, minutes, and seconds. In this report, queried data should be unique per Picking + Packing **\[P1\] Customer/Recipient Name** **Warehouse Name** **Outbound ID** **Outbound Closing time** – One Outbound multiple Picking will have same value **Picking ID** – If an Outbound has multiple picking events, have multiple rows (according to the Picking) **\[P1\] Picker Name** **Picking Start** **Picking Finish** **\[P1\] Packer Name** **Packing ID** **Packing Start** **Packing Finish** **\[P1\] Shipping PIC** **\[P1\] Courier Name** **Shipping ID** **Shipping At** Edge cases Missing stage timestamps show as blank, not zero or epoch | **AC#1 Columns present** **GIVEN** the raw data view, **WHEN** it loads, **THEN** it shows Warehouse Name, Outbound ID, Outbound Closing time, Picking ID, Picker Name (P1), Picking Start, Picking Finish, Packer Name (P1), Packing ID, Packing Start, Packing Finish, Shipping At.   **AC#2 Row granularity** **GIVEN** an outbound with multiple picking or packing events, **WHEN** raw data loads, **THEN** there is one row per Picking (and per Packing where applicable), with outbound-level fields repeated.   **AC#3 Blank not zero** **GIVEN** an outbound with a stage not yet done, **WHEN** raw data shows that timestamp, **THEN** the cell is blank, not zero or epoch. | \[Jira\] |
| 14 | Outbound Accuracy — raw data _**As an**_ analyst, _**I want**_ row-level outbound accuracy data I can export, _**so that**_ I can trace fulfilment down to each outbound and SKU. | <custom data-type="status" data-id="id-27">Must Have</custom> | \[TBD\] | Filters Date (range, default D-6 until D, must fill, max 366 days) Warehouse Dropdown of “All”, and list of active Warehouse Name Default value For Warehouse Manager → Warehouse that is first assigned to that user (earliest created_at for user x warehouse assignment) For Owner and/or ~~LM~~ Ultimate → “All” _Notes: LM role is removed per 30 July 2026 due to its actual capability, which is to enable the user to do create and update action. Create and update action is not relevant for this page_ Filter must not be null for the data to be able to load SKU (default "All"; either "All" or 1 <= x <= 10 SKUs) Operator (Picker / Packer / Shipping PIC) Dropdown of Warehouse Manager, Warehouse Operator, Not assigned, and “all” Can only select one value Operator filter WILL NOT apply to Pending Outbound and Closed Outbound If value = all, then show all data without operator filter If value = Not assigned, then show data with no assignee yet Raw data columns Get all Outbound which is closed within the filtered date. In this report, queried data should be unique per Product + Picking + Packing **\[P1\] Customer/Recipient Name** **Warehouse Name** **Outbound ID** – One Outbound with multiple SKU lines will have multiple rows **Product Name** – Will only have value if the SKU filter != All. Otherwise, left blank **Outbound Qty** **\[P1\] Picker Name** **Picking ID** **Picked Qty** **\[P1\] Packer Name** **Packing ID** **Packed Qty** **\[P1\] Shipping PICPIC 1\] Courier Name** **Shipping ID** **Shipped Qty** | **AC#1 Row grain** **GIVEN** an outbound with multiple SKU lines or picking events, **WHEN** raw data loads, **THEN** rows are at Outbound x SKU (and per Picking) grain and reflect the filters.   **AC#2 Columns present** **GIVEN** the raw data view, **WHEN** it loads, **THEN** it shows Customer/Recipient Name (P1), Warehouse Name, Outbound ID, Product Name, Outbound Qty, Picker Name (P1), Picking ID, Picked Qty, Packer Name (P1), Packing ID, Packed Qty, Courier Name (P1), Shipping ID, Shipped Qty.   **AC#3 Product Name per SKU filter** **GIVEN** the SKU filter is "All", **WHEN** raw data loads, **THEN** Product Name is left blank; when specific SKUs are selected, Product Name is populated. | \[Jira\] |

**Scope boundary.** This PRD covers Inbound (Timeliness, Accuracy), Outbound (Timeliness, Accuracy), Product Issuance Rate (Stock Velocity), and Inventory Accuracy (cycle count variance), delivered across an Analytics page and a Reports page. Inventory Accuracy depends on the Cycle Count / Stock Opname feature and reason-coded adjustment (P0 anchor items) being available. Do not confuse Inbound/Outbound Accuracy (a process measure: received or shipped vs expected) with Inventory Accuracy (an inventory measure: system vs physical on-hand).

# Impacted Module Areas

| Module | Impact |
| --- | --- |
| Fulfillment (WMS) | Primary source. Reads inbound, outbound, receiving, putaway, picking, packing, shipping, and cycle count records with their timestamps and operators. New reporting aggregations, an operational live view, and a raw data export are new WMS artifacts. |
| Product & Inventory | Reads product master (SKU, tracking method, base UOM), on-hand stock-by-location, and stock movements (for issuance velocity). Cycle count results and reason-coded adjustments feed Inventory Accuracy. |
| Transaction | Reads expected qty from the linked PO or ASN (inbound) and SO (outbound) for the accuracy fill rate. |
| Accounting (JE) | None. These reports are read-only analytics with no journal entries. Value variance in Inventory Accuracy reads unit cost only, it does not post. |
| Data / Analytics | New aggregation queries or read models, a near-real-time operational view, plus an export service. |
| AI | None in MVP. Operator, duration, and velocity data can seed future performance and FSN analytics. |

# Open Questions

Operational metrics: exact state definitions (waiting to receive, in receiving, waiting for putaway, waiting to pick/pack/ship). Needs one agreed rule per state.Operational refresh cadence: on page load vs live polling; and the exact date scope (strictly today vs all currently-open work regardless of date).Deep-link filter mapping: exactly which filters carry over on each card click (confirm per metric).Operator filters (Receiver, Putaway PIC, Picker, Packer, Courier): confirm as P1 and where they appear (Operational "my queue" and Reports raw).Product Issuance on the Analytics page: dashboard headline cards vs the per-product table; and where the per-product table lives (Reports page).Inventory Accuracy scorecard: finalize the metric (currently "xxx" placeholder) and define the matching raw data columns.Reporting grain: header level or line level. Recommendation: line-level base (inbound by SKU), rolled up to header for the summary.Duration statistic: average, median, or p90. Recommendation: show median and p90, not average.Date filter basis: inbound created date, receiving date, or completion date. Recommendation: inbound created date, with a completion-date view as a later option.Volume headline meaning: Total Receiving and Total Putaway as funnel counts of inbounds, or as counts of receiving and putaway events. Recommendation: funnel counts of inbounds.Definition of Completed Inbound: all lines received to expected qty, or status Closed. Needs one agreed rule.Over-receipt handling in Receive rate: cap at 100% or show the true rate. Recommendation: show the true rate and flag.Returns (Inbound Return): included in these inbound reports or reported separately.Damaged units: counted in Receive qty, or excluded pending QC result.Report location and RBAC roles: confirm placement in the WMS module and the viewer roles (Owner, LM, Stockist).Add Operator columns (receiver, putaway operator) to the timeliness raw data, to seed the future Employee Performance report at low incremental cost.Product Issuance Rate name: keep, or rename to "Stock Velocity & Deadstock". Recommendation: rename.Issuance definition: outbound shipments only, or all stock-out movements (transfer-out, negative adjustment).Deadstock window and Issuance Rate unit: default no issuance in 90 days; rate per day vs per week. Both configurable.Inventory Accuracy depends on the Cycle Count / Stock Opname feature and reason-coded adjustment (P0 anchor). Confirm those ship before or with this report.Inventory Accuracy value variance depends on unit cost (batch/serial costing). Confirm availability, else keep value metrics as P1.Variance by ABC class in Inventory Accuracy: deferred to Next Phase pending the ABC / FSN engine.Outbound Fulfillment State definition (MATCH / SHORT / OVER EXPECTED): confirm the rule and whether it is measured at picked, packed, or shipped qty.


